# --- build outputs ---
/digit
/digit.exe
/digit-report.html
/digit-report.json
*.o
*.exe
*.exe~
*.test
*.out
/dist/

# frontend build output — generated by `pnpm run build`, embedded via
# go:embed at Go build time. Must exist before `go build .` will succeed;
# run `make build-web` (or `make build`) first. Never commit this.
web/dist/

# --- Node / pnpm ---
web/node_modules/
web/tsconfig.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# --- editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo

# --- OS cruft ---
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- env / secrets ---
.env
.env.local
.env.*.local
